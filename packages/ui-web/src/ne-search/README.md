# NeSearch

Collapsible search container for list pages.

- Renders a unified search area header and body
- Supports expand/collapse via configurable labels
- Exposes `onReset` so pages can decide how to clear filters and whether to refetch
- Intended for filter forms and lightweight status/error hints